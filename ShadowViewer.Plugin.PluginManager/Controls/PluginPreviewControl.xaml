<UserControl
    x:Class="ShadowViewer.Plugin.PluginManager.Controls.PluginPreviewControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    xmlns:converters1="using:ShadowViewer.Sdk.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:fluent="using:FluentIcons.WinUI"
    xmlns:helpers="using:ShadowViewer.Plugin.PluginManager.Helpers"
    xmlns:i18N="using:ShadowViewer.Plugin.PluginManager.I18n"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:ShadowViewer.Plugin.PluginManager.Models"
    mc:Ignorable="d">
    <Grid
        Width="344"
        Height="244"
        RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="64" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid
                Grid.Column="0"
                Width="64"
                Height="64"
                Background="{ThemeResource SolidBackgroundFillColorTertiaryBrush}"
                BorderBrush="{ThemeResource SolidBackgroundFillColorSecondaryBrush}"
                BorderThickness="1"
                CornerRadius="4">
                <Viewbox Width="54" Height="54">
                    <ContentPresenter Content="{x:Bind helpers:PluginHelper.GetLoginIcon(ViewModel), Mode=OneWay}" />
                </Viewbox>

            </Grid>
            <Grid
                Grid.Column="1"
                Padding="0,4"
                RowSpacing="6">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock
                    Grid.Row="0"
                    FontSize="19"
                    FontWeight="Bold"
                    Text="{x:Bind ViewModel.MetaData.Name, Mode=OneWay}"
                    TextTrimming="CharacterEllipsis" />
                <TextBlock
                    Grid.Row="1"
                    FontSize="16"
                    Foreground="Gray"
                    Text="{x:Bind ViewModel.MetaData.Authors, Mode=OneWay}"
                    TextTrimming="CharacterEllipsis" />
            </Grid>
            <HyperlinkButton
                Grid.Column="2"
                Width="37"
                Height="37"
                Padding="5"
                VerticalAlignment="Top"
                NavigateUri="{x:Bind ViewModel.MetaData.WebUri, Mode=OneWay}">
                <ImageIcon
                    Width="25"
                    Height="25"
                    Source="{ThemeResource GithubIcon}" />
            </HyperlinkButton>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel
                Grid.Column="0"
                Orientation="Horizontal"
                Spacing="4">

                <ToolTipService.ToolTip>
                    <TextBlock>
                        <Run Text="Id" />
                        <Run Text=": " />
                        <Run Text="{x:Bind helpers:PluginNameHelper.GetPluginId(ViewModel.MetaData.Id), Mode=OneWay}" />
                    </TextBlock>
                </ToolTipService.ToolTip>
                <fluent:SymbolIcon
                    FontSize="14"
                    Foreground="Gray"
                    Symbol="PuzzlePiece" />
                <TextBlock Foreground="Gray" Text="{x:Bind helpers:PluginNameHelper.GetPluginId(ViewModel.MetaData.Id), Mode=OneWay}" />
            </StackPanel>
            <StackPanel
                Grid.Column="2"
                Orientation="Horizontal"
                Spacing="4">

                <ToolTipService.ToolTip>
                    <TextBlock>
                        <Run Text="{i18N:Locale Key=Version}" />
                        <Run Text=": " />
                        <Run Text="{x:Bind ViewModel.MetaData.Version, Mode=OneWay}" />
                    </TextBlock>
                </ToolTipService.ToolTip>
                <fluent:SymbolIcon
                    FontSize="14"
                    Foreground="Gray"
                    Symbol="Lightbulb" />
                <TextBlock Foreground="Gray" Text="{x:Bind ViewModel.MetaData.Version, Mode=OneWay}" />
            </StackPanel>
        </Grid>
        <TextBlock
            Grid.Row="2"
            FontSize="17"
            Text="{x:Bind ViewModel.MetaData.Description, Mode=OneWay}"
            TextTrimming="CharacterEllipsis"
            TextWrapping="Wrap" />
    </Grid>
</UserControl>